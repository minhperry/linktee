# Linktee

As from the name, you can guess it's a Linktr.ee clone. 

# Features

- Buttons to redirect to your social media profiles
- (more to come)

# Installation

1. Clone the repository
2. Run `npm install`
3. Run `npm run dev`
4. Visit `http://localhost:5173`

Datas are defined in [buttonData.ts](src/data/buttonData.ts)